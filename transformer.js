export function transform(el, message) {
    const target = message.isArray ? [] : {};
    debugger;
}
function doTransform(target, el, transform, context) {
    el.children.forEach(child => {
        for (const key in transform) {
            if (key[0] === key[0])
                ;
        }
    });
}
